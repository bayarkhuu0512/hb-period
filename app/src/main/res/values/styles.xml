<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="BgOvalStyle">
        <item name="android:layout_height">@dimen/bg_icon_size</item>
        <item name="android:layout_width">@dimen/bg_icon_size</item>
        <item name="android:padding">@dimen/bg_icon_padding</item>
        <item name="android:layout_margin">@dimen/bg_icon_padding</item>
    </style>

    <style name="BgOvalPeriodStyle" parent="BgOvalStyle">
        <item name="android:background">@drawable/bg_oval_period</item>
    </style>

</resources>
